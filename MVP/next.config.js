/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js 16: Turbopack is now the default for both dev and build.
  // No need to pass --turbopack flag anymore.
  // If you have a custom webpack config, use: next build --webpack
};

module.exports = nextConfig;
